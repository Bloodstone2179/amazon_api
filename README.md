# amazon_api

amazonTest is the module name to get the request (Returns a tuple [0]=price,[1]=name, [2]=product code,[3]=url 

amazonApi is the api script file using flask that runs on 127.0.0.1:5000/api/<productCode>
  
api is how you interact with amazonTest to get the results
  
  
